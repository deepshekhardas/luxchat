import { describe, it, expect } from 'vitest';

// Simple smoke test to verify test runner works
describe('App Component', () => {
  it('should pass a basic truthy test', () => {
    expect(true).toBe(true);
  });
});
